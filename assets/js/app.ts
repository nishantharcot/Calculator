(
  document.onreadystatechange = (): void => {
    if (document.readyState === 'complete') {
      const model = {

      };
      const view = {

      };
      const controller = {
        init: (): void => {
          // To be done
        },
      };
      controller.init();
    }
  }
)();
